import React from 'react'
import { Helmet } from 'react-helmet'

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name='description' content={description} />
      <meta name='keyword' content={keywords} />
    </Helmet>
  )
}

Meta.defaultProps = {
  title: 'Free Learning Blog',
  description: 'Learning for free',
  keywords: 'Learning, HTML, CSS, JS, React, Javascript',
}

export default Meta
